spring:
  rabbitmq:
    password: 'secret'
    username: 'myuser'

bot:
  twitch:
    oauth:
      token: ${TWITCH_OAUTH_TOKEN}
  subathon:
    # Expects a comma separated list of channel names to join
    channels: 
    command:
      prefix: '!'

logging:
  level:
    com:
      pronixxx:
        subathon:
          bot: trace